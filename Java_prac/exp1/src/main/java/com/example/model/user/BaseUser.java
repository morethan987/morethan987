package com.example.model.user;

import java.util.Map;

class BaseUser {

    /**
     * Read date from a file
     * @param file
     * @return the map contains the data read from the file, organized by the file columns: <column name, column values[]>
     */
    public Map<String, String[]> readFile(String file) {
        return null;
    }
}
